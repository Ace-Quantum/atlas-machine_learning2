#!/usr/bin/env python3

"""Documentation"""

import tensorflow.keras as K


def predict(network, data, verbose=True):
    """Documentation"""

    predictions = network.predict(data)

    return predictions
